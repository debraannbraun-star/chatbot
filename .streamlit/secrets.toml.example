# Example Streamlit secrets file.
# Copy this to `.streamlit/secrets.toml` (DO NOT commit the real file with secrets)
# Replace the placeholders with your actual secret values.

# OpenAI API key (if your app uses OpenAI)
# OPENAI_API_KEY = "sk-..."

# Any other environment secrets your app needs, for example:
# DATABASE_URL = "postgres://user:pass@host:port/dbname"
# SOME_OTHER_SECRET = "value"

# Notes:
# - On Streamlit Community Cloud you can instead set these values in the app "Secrets" UI.
# - Never commit `.streamlit/secrets.toml` to the repository.
